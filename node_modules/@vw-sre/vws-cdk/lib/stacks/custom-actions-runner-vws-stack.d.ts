import { Stack, StackProps } from 'aws-cdk-lib';
import { Policy } from 'aws-cdk-lib/aws-iam';
import { Construct } from 'constructs';
export interface DeploymentPolicyProps {
    stackName: string;
    roleArn: string;
}
export interface CustomActionsRunnerVwsStackProps extends StackProps {
    roleArn: string;
    privateKeySecret: string;
    privateKeyCmkArn: string;
    version?: string;
    runnerName?: string;
    runnerTag?: string;
    githubOrg: string;
    githubRepo: string;
    githubAppId: string;
    suffix?: string;
}
export declare class CustomActionsRunnerVwsStackDeploymentPolicy extends Policy {
    constructor(scope: Construct, id: string, props: DeploymentPolicyProps);
}
export declare class CustomActionsRunnerVwsStack extends Stack {
    constructor(scope: Construct, id: string, props: CustomActionsRunnerVwsStackProps);
}
