import { Stack, StackProps } from 'aws-cdk-lib';
import { IVpc } from 'aws-cdk-lib/aws-ec2';
import { ManagedPolicy } from 'aws-cdk-lib/aws-iam';
import { Construct } from 'constructs';
import { RetentionDays } from 'aws-cdk-lib/aws-logs';
export interface LogGroupManagerStackProps extends StackProps {
    vpc?: IVpc;
    logRetentionPeriod?: RetentionDays;
}
export declare class LogGroupManagerStack extends Stack {
    constructor(scope: Construct, id: string, props?: LogGroupManagerStackProps);
}
interface LogGroupManagerDeploymentPolicyProps {
    stackName: string;
    managedPolicyName?: string;
}
export declare class LogGroupManagerDeploymentPolicy extends ManagedPolicy {
    constructor(scope: Construct, id: string, props: LogGroupManagerDeploymentPolicyProps);
}
export {};
