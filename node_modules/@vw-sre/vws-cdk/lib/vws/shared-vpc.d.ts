import { Resource, ResourceProps } from 'aws-cdk-lib';
import { IVpc } from 'aws-cdk-lib/aws-ec2';
import { Construct } from 'constructs';
import { CfnShared } from '../generated';
export interface SharedVpcProps extends ResourceProps {
    availabilityZones?: string[];
}
export declare class SharedVpc extends Resource {
    static from(sharedVpc: CfnShared, availabilityZones: string[]): IVpc;
    readonly vpc: IVpc;
    constructor(scope: Construct, id: string, props?: SharedVpcProps);
}
