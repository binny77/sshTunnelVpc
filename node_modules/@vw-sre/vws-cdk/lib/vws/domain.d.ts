import { HostedZone, IHostedZone } from 'aws-cdk-lib/aws-route53';
import { Construct } from 'constructs';
/**
 * @field domain: domain to request, if not defined, a random domain will be allocated
 */
export interface DomainProperties {
    readonly domain: string;
}
/**
 * Domain takes care of all domain related resources, it will create:
 *
 * - A domain entry in the vwapps.run registrar
 * - A domain delegation request
 * - A hosted zone to handle the registered domain
 */
export interface IDomain {
    /**
     * The HostedZone that is used for the domain.
     */
    hostedZone: IHostedZone;
    /**
     * The name of the domain
     */
    name: string;
}
export declare class Domain extends Construct implements IDomain {
    readonly hostedZone: HostedZone;
    readonly name: string;
    constructor(scope: Construct, id: string, props?: DomainProperties);
}
